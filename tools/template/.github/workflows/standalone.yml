##======================================================================================================================
##  SAMPLE - @brief@
##  Copyright : SAMPLE Project Contributors
##  SPDX-License-Identifier: BSL-1.0
##======================================================================================================================
## Republishes the single header on the standalone branch, which has to exist before the first run.
name: SAMPLE Standalone
on:
  push:
    branches:
      - main

jobs:
  standalone:
    permissions:
      contents: write
      pull-requests: write
    uses: jfalcou/copacabana/.github/workflows/package-standalone.yml@44053e713b1b57329ccbcbb8878c839024bf19ee # v8
    with:
      project: sample
